<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <PropertyGroup>
    <RootNamespace>QueryFarm.Vgi.Tests</RootNamespace>
    <IsPackable>false</IsPackable>
    <!-- xUnit1051: prefer TestContext.Current.CancellationToken over `default` — not worth
         the churn for these small synchronous-logic tests. -->
    <NoWarn>$(NoWarn);xUnit1051</NoWarn>
    <!-- JIT every method optimized on first call instead of tiering up from unoptimized code.
         Some bugs exist only in optimized code — an object the optimizer stops reporting live
         while memory it owns is still in use (Protocol/EmbeddedIpcCollectionTests.cs) — and under
         the default tiering a test for one passes until the method happens to be promoted. -->
    <TieredCompilationQuickJit>false</TieredCompilationQuickJit>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit.v3" />
    <PackageReference Include="xunit.runner.visualstudio">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="coverlet.collector">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/QueryFarm.Vgi/QueryFarm.Vgi.csproj" />
    <ProjectReference Include="../../fixtures/QueryFarm.Vgi.SimpleWritableWorker/QueryFarm.Vgi.SimpleWritableWorker.csproj" />
    <ProjectReference Include="../../fixtures/QueryFarm.Vgi.ExampleWorker/QueryFarm.Vgi.ExampleWorker.csproj" />
  </ItemGroup>

  <!-- WorkerUnixSocketTests.cs drives Worker.RunUnixSocketAsync with a real client — needs
       QueryFarm.VgiRpc.Client directly, not just the QueryFarm.Vgi surface QueryFarm.Vgi.csproj
       already pulls in. Same sibling-source-vs-NuGet split as that project (see root
       Directory.Build.props). -->
  <ItemGroup Condition="'$(VgiRpcCSharpUseSource)' == 'true'">
    <ProjectReference Include="$(VgiRpcCSharpRoot)/src/QueryFarm.VgiRpc.Client/QueryFarm.VgiRpc.Client.csproj" />
  </ItemGroup>
  <ItemGroup Condition="'$(VgiRpcCSharpUseSource)' != 'true'">
    <PackageReference Include="QueryFarm.VgiRpc.Client" />
  </ItemGroup>

</Project>
